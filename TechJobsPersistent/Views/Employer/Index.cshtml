@using TechJobsPersistent.ViewModels;
@model List<TechJobsPersistent.Models.Job>

<h1>Employers Currently Hiring</h1>

@if (Model.Count == 0)
{
    <p>No employers are currently hiring!</p>
}
else
{
    <div>
        <ul>
            @foreach (Job jobs in Model)
            {
                <li>
                    <a asp-controller="Employer" asp-action="About" asp-route-id="@jobs.Id">@jobs.Name</a>
                </li>
            }
        </ul>
    </div>
}